# .env のサンプル。コピーして使用してください:
#   cp .env.example .env  （.env は .gitignore で除外されコミットされません）
# Vagrantfile が起動時に自動で読み込みます。

# MariaDB の root パスワード（非機能要件: セキュリティ）
DB_ROOT_PASS=change_this_strong_password

# アプリ用 DB ユーザー appuser のパスワード
DB_APP_PASS=change_this_app_password
